#unique values => .unique()
#sorting => sorted(Dataframe)
#get to str len => on column .str.len()
#return in format res[[column1,column2]]
#mean of different idd --> pd.DataFrame(res.groupby('machine_id')['processing_time'].mean()